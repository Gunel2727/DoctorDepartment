@model List<Doctor>

@if (Model.Count == 0)
{
	<h2>No doctors found in this department</h2>
}
else
{
	@foreach (var doctor in Model)
	{
		<div>
			<h3>FullName: @doctor.FullName</h3>
			<p>Specialization: @doctor.Specialty</p>
		</div>
	}

}
